name: Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache: false

      - name: Build
        run: go build -ldflags="-X main.version=${GITHUB_REF_NAME#v}" -o pink-elevenlabs-darwin-arm64 .

      - name: Test run
        run: ./pink-elevenlabs-darwin-arm64 --version

      - uses: actions/upload-artifact@v6
        with:
          name: pink-elevenlabs-darwin-arm64
          path: pink-elevenlabs-darwin-arm64

  build-macos-intel:
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache: false

      - name: Build
        run: go build -ldflags="-X main.version=${GITHUB_REF_NAME#v}" -o pink-elevenlabs-darwin-amd64 .

      - name: Test run
        run: ./pink-elevenlabs-darwin-amd64 --version

      - uses: actions/upload-artifact@v6
        with:
          name: pink-elevenlabs-darwin-amd64
          path: pink-elevenlabs-darwin-amd64

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache: false

      - name: Build
        run: go build -ldflags="-X main.version=${GITHUB_REF_NAME#v}" -o pink-elevenlabs-linux-amd64 .

      - name: Test run
        run: ./pink-elevenlabs-linux-amd64 --version

      - uses: actions/upload-artifact@v6
        with:
          name: pink-elevenlabs-linux-amd64
          path: pink-elevenlabs-linux-amd64

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.24'
          cache: false

      - name: Build
        run: go build -ldflags="-X main.version=${GITHUB_REF_NAME#v}" -o pink-elevenlabs-windows-amd64.exe .

      - name: Test run
        run: ./pink-elevenlabs-windows-amd64.exe --version

      - uses: actions/upload-artifact@v6
        with:
          name: pink-elevenlabs-windows-amd64.exe
          path: pink-elevenlabs-windows-amd64.exe

  release:
    needs: [build-macos, build-macos-intel, build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v6
        with:
          path: dist
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          name: pink-elevenlabs ${{ github.ref_name }}
          body_path: RELEASE_NOTES.md
          files: dist/*
          make_latest: true
